import 'package:flutter/material.dart';

class FovoritesPage extends StatelessWidget {
  const FovoritesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Fovorite_page'),
    );
  }
}
